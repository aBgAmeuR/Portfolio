{
  "name": "portfolio",
  "version": "3.0.0",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "deploy": "turbo deploy"
  },
  "devDependencies": {
    "turbo": "^2.5.3"
  },
  "packageManager": "pnpm@8.7.4",
  "dependencies": {
    "dotenv": "^16.4.7",
    "dotenv-cli": "^8.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}