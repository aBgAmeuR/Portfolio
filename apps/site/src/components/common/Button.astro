---
import { Icon } from 'astro-icon/components'

export interface Props {
  href: string;
  icon?: string;
}

const { href, icon } = Astro.props;
---

<a 
  href={href} 
  target="_blank" 
  rel="noreferrer"
  class="px-4 py-2 text-sm rounded-xl inline-flex items-center justify-center gap-2 whitespace-nowrap font-medium transition-colors bg-primary text-primary-foreground hover:bg-primary/90 outline-none focus-visible:ring-2 focus-visible:ring-ring ring-offset-background focus-visible:ring-offset-2"
>
  {icon && <Icon name={icon} class="pointer-events-none shrink-0 size-4" />}
  <slot />
</a>
